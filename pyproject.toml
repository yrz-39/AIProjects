[project]
name = "ai-study-assistant"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.140.7",
    "uvicorn[standard]>=0.51.0",
    "python-multipart>=0.0.20",
    "openai>=2.53.0",
    "python-dotenv>=1.2.2",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
